# Kinglet language server + VS Code extension (Perch).

# Bootstrap GN links (symlink/junction via scripts/setup-deps)
/build/config
/build/toolchain
/build/*.gni
/compiler

# Build outputs
/out/
bin/

/node_modules/
*.vsix
.DS_Store
test.kl
tmp-err.txt
tmp-*.txt
